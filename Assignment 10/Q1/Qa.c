// Define a structure named Person with its members: Person(name, age, address, contact)
#include <stdio.h>

struct person{
  char name[20];
  int age;
  char address[40];
  int contact;
}

int main() {

  return 0;
}
